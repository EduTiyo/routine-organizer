## Do

- use Shadcn for components.
- use tailwind for css
- modularize componentes as in components/StudentCardList.tsx, components/StudnetCard.tsx
- use Skeletons while loading the data. See an example in app/(private)/dashboard/professor/page.tsx and components/StudentListSkeleton.tsx

## Don't

- dont hard code colors
- dont use divs if we have already a componente

## Project Structure

- See app/api/ for our routes
- Components are in components/
- Database schema is in prisma/schema.prisma
